def sum_of_integers(n):
'''(int) -> (int)
Returns the sum of the digits of a positive integers
'''
    if n == 0:
        return n
    else:
        return n % 10 + sum_of_integers(n // 10)

#print sum_of_integers(10463235)def sum_of_integers(n):
 
